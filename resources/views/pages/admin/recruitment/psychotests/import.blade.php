@extends('layouts.app')
@section('content')
<x-common.page-breadcrumb pageTitle="Import Soal PAPI" />
<div class="space-y-6"><div class="rounded-2xl border border-gray-200 bg-white p-5"><div class="flex flex-col gap-3 sm:flex-row sm:justify-between"><div><h2 class="font-semibold">Versi {{ $version->version }}</h2><p class="mt-1 text-sm text-gray-500">Import akan mengganti seluruh soal versi draft ini dalam satu transaksi.</p></div><a href="{{ route('admin.recruitment.psychotests.questions.template') }}" class="crud-btn-secondary">Unduh Template</a></div><form method="POST" enctype="multipart/form-data" action="{{ route('admin.recruitment.psychotests.questions.import.preview',$version) }}" class="mt-5">@csrf<input type="file" name="question_file" accept=".xlsx,.xls,.csv" required class="block w-full rounded-lg border border-gray-300 px-3 py-2.5 text-sm"><div class="mt-4 flex justify-end"><button class="crud-btn-primary">Buat Preview</button></div></form></div>
@if(is_array($previewRows))@php($invalid=collect($previewRows)->where('valid',false)->count())<div class="rounded-2xl border border-gray-200 bg-white"><div class="border-b border-gray-200 px-5 py-4"><h2 class="font-semibold">Preview {{ count($previewRows) }} Baris</h2><p class="mt-1 text-sm text-gray-500">{{ $invalid }} baris bermasalah.</p></div><div class="max-h-[560px] overflow-auto"><table class="min-w-full divide-y divide-gray-200"><thead class="sticky top-0 bg-gray-50"><tr>@foreach(['No','Pernyataan A','Pernyataan B','Validasi'] as $h)<th class="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-gray-100">@foreach($previewRows as $row)<tr><td class="px-4 py-3 font-medium">{{ $row['number'] }}</td><td class="px-4 py-3 text-sm">{{ $row['statement_a'] }}</td><td class="px-4 py-3 text-sm">{{ $row['statement_b'] }}</td><td class="px-4 py-3 text-xs">{{ $row['valid']?'Valid':implode(' ', $row['errors']) }}</td></tr>@endforeach</tbody></table></div><div class="flex justify-end border-t border-gray-200 px-5 py-4"><form method="POST" action="{{ route('admin.recruitment.psychotests.questions.import.store',$version) }}">@csrf<button class="crud-btn-primary" @disabled($invalid)>Konfirmasi Import 90 Soal</button></form></div></div>@endif</div>
@endsection
